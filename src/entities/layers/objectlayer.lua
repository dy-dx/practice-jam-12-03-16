local ObjectLayer = Class{}

function ObjectLayer:init(x, y, dir)
end

function ObjectLayer:reset(x, y, dir)

end

function ObjectLayer:draw()
end

function ObjectLayer:update(dt)
end

return ObjectLayer
